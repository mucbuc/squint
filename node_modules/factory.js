function Factory() {
  
  var map = {
      header: 'header', 
      footer: 'footer', 
      seperator: 'seperator', 
    };

  this.create = function( name ) {
    return map[ name ];
  };
  
}

module.exports.Factory = Factory;